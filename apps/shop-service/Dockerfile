# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @eshopper/seller-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t eshopper-seller-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system eshopper-seller-service && \
          adduser --system -G eshopper-seller-service eshopper-seller-service

COPY apps/seller-service/dist eshopper-seller-service/
COPY apps/seller-service/package.json eshopper-seller-service/
RUN chown -R eshopper-seller-service:eshopper-seller-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix eshopper-seller-service --omit=dev -f install

CMD [ "node", "eshopper-seller-service" ]
