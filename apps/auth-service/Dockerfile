# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @eshopper/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t eshopper-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system eshopper-auth-service && \
          adduser --system -G eshopper-auth-service eshopper-auth-service

COPY apps/auth-service/dist eshopper-auth-service/
COPY apps/auth-service/package.json eshopper-auth-service/
RUN chown -R eshopper-auth-service:eshopper-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix eshopper-auth-service --omit=dev -f install

CMD [ "node", "eshopper-auth-service" ]
